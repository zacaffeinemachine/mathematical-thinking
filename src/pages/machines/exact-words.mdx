---
layout: ../../layouts/Chapter.astro
title: Exact Words
order: 7
chapter: machines
chapterTitle: Machines
blurb: Machines that accept a short list of words and nothing else, over an alphabet of twenty-six letters.
description: "Design-a-machine problems on recognising exact words over a large alphabet."
---
import { Problem, Tests } from "../../components/games/fsm/Problem.tsx";

<Problem n={1} title="One short word" alphabet="the 26 lowercase letters">
Design a machine that accepts `hat` and nothing else. Draw one arrow labelled
`x` for "any other letter" rather than drawing twenty-six of them.
<Tests accept={["hat"]} reject={["", "ha", "hate", "hatt", "cat", "aht"]} />
</Problem>

<Problem n={2} title="One longer word" alphabet="the 26 lowercase letters">
Design a machine that accepts `code` and nothing else. Draw one arrow labelled
`x` for "any other letter" rather than drawing twenty-six of them.
<Tests accept={["code"]} reject={["", "cod", "codes", "coda", "ccode", "node"]} />
</Problem>

<Problem n={3} title="Two words that share nothing" alphabet="the 26 lowercase letters">
Design a machine that accepts `hate` and `qrmt`, and nothing else. Draw one
arrow labelled `x` for "any other letter" rather than drawing twenty-six of
them.
<Tests accept={["hate", "qrmt"]} reject={["", "hat", "qrm", "hateqrmt", "haterqrmt", "hqartme"]} />
</Problem>

<Problem n={4} title="Three words, two of which start alike" alphabet="the 26 lowercase letters">
Design a machine that accepts `start`, `stop` and `pause`, and nothing else.
Draw one arrow labelled `x` for "any other letter" rather than drawing
twenty-six of them.
<Tests accept={["start", "stop", "pause"]} reject={["", "st", "sto", "starts", "stopp", "paus", "startstop"]} />
</Problem>

<Problem n={5} title="Words that are prefixes of each other" alphabet="the 26 lowercase letters">
Design a machine that accepts `to`, `too` and `tool`, and nothing else. Draw
one arrow labelled `x` for "any other letter" rather than drawing twenty-six
of them.
<Tests accept={["to", "too", "tool"]} reject={["", "t", "tools", "toll", "tooll", "ot"]} />
</Problem>

<Problem n={6} title="Every short word" alphabet="the 26 lowercase letters">
Design a machine that accepts every string of length 2 or less, over the 26
letters, and nothing longer. Draw one arrow labelled `x` for "any other
letter" rather than drawing twenty-six of them.
<Tests accept={["", "a", "z", "hi", "qq"]} reject={["cat", "hate", "abcd", "aaa"]} />
</Problem>

<Problem n={7} title="A word at the start, anything after" hard alphabet="the 26 lowercase letters">
Design a machine that accepts exactly the strings that begin with `un`. Draw
one arrow labelled `x` for "any other letter" rather than drawing twenty-six
of them.
<Tests accept={["un", "und", "undo", "unhappy", "unn"]} reject={["", "u", "n", "nu", "sun", "aun"]} />
</Problem>

<Problem n={8} title="A word repeated" hard alphabet="{a, b}">
Design a machine that accepts exactly the strings made by writing `ab`
some number of times over: the empty string, `ab`, `abab`, `ababab`, and so
on.
<Tests accept={["", "ab", "abab", "ababab"]} reject={["a", "b", "ba", "aab", "aba", "abb"]} />
</Problem>

<Problem n={9} title="No letter twice in a row" hard alphabet="{a, b}">
Design a machine that accepts exactly the non-empty strings in which no two
neighbouring letters are the same.
<Tests accept={["a", "b", "ab", "ba", "abab", "babab"]} reject={["", "aa", "bb", "ababb", "abba"]} />
</Problem>
